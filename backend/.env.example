# Frontend origin for CORS (adjust if needed)
FRONTEND_ORIGIN=http://localhost:5173

# Path to your trained model (optional). If missing, fallback rule-based score is used.
MODEL_PATH=models/resume_ai_score_reg_xgb.pkl

# Optional: enable AI feedback using OpenAI
OPENAI_API_KEY=
